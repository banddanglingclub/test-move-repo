{
  "routes": [
    {
      "route": "/new/{*path}",
      "rewrite": "/new/index.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [
      "/assets/*",
      "/*.{css,scss,js,png,jpg,gif,ico,svg,woff2}",
      "/new/*"
    ]
  }
}
