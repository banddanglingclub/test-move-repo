namespace AnglingClubWebsite.IndexTemplates
{

  /* TODO Ang to Blazor Migration - in IndexTemplates
   * This folder only required until the migration to Blazor is complete.
   * Once done, just keep index.dev.html and rename to index.html in wwwroot.
   */
}
